describe('Express.js Dummy Tests', () => {

  // ✅ Dummy test to ensure Jest is working properly
  test('2 + 2 should be 4', () => {
    expect(2 + 2).toBe(4);
  });

  test('another test that returns true', () => {
    expect(true);
  });

});
